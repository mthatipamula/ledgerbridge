spring:
  ai:
    mcp:
      client:
        enabled: true
        name: ledgerbridge-mcp-client
        version: 1.0.0
        type: SYNC
        streamable-http:
          connections:
            ledgerbridge-server:
              url: http://localhost:8080
              endpoint: /mcp